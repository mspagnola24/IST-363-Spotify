import styles from '.paragraph.module.scss'

let cx = className.bind(styles)

const paragraph = ({children, intro}) => {
    let paragraph 
    return <p>paragraph goes here</p>
}
export default paragraph; 